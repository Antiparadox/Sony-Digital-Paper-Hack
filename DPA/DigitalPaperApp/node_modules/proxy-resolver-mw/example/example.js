
process.env.DEBUG = '*';

const proxyResolver = require('../index.js');

const targetUrl = 'http://info.update.sony.net/DR001/DigitalPaperApp_W/info/info.xml';
const userAgent = 'DPA_1.0.0.10122#DigitalPaper#1.2.00.11220#00#-DaM_uAS-#http.status.0#';

proxyResolver.getProxy(targetUrl, userAgent, function (error, proxy) {
  if (error) {
    console.error(error);
    return;
  }
  console.log('Proxy: ' + proxy);
});
