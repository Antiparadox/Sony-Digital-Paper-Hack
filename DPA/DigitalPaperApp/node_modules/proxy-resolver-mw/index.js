'use strict';

/** @exports proxy-resolver-mw */
const proxyResolver = exports;


/**
 * 指定した URL に接続する際に使用するプロキシサーバーの設定を取得する。
 *
 * @param {string} targetUrl
 *    接続先の URL 。
 * @param {string} userAgent
 *    接続時の User-Agent 。
 * @param {function(?Error, string)} callback
 *    プロキシサーバーの取得結果を受け取るするための callback 関数。
 *    取得に失敗した場合には、第一引数に Error オブジェクトが渡される。
 *    取得に成功した場合には、第一引数に null が、第二引数にプロキシサーバーの
 *    文字列が渡される。プロキシサーバーが設定されていない場合には、空文字列が
 *    渡される。
 */
proxyResolver.getProxy = function (targetUrl, userAgent, callback) {
   require('./build/Release/proxy_resolver').getProxy(targetUrl, userAgent, callback);
}

