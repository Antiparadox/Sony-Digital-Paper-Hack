
process.env.DEBUG = "*:error";

'use strict';

var UsbEthernetFinder = require('../index.js');

UsbEthernetFinder.find(0x054C, 0x0BDD, function(err, result) {
    if (!err) {
        console.log(result);
    }
});
